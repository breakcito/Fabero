import { Outlet } from "react-router-dom";

export const ConfiguracionLayout = () => {
  return <Outlet />;
};

export const EmpresasLayout = () => {
  return <Outlet />;
};

export const PersonalLayout = () => {
  return <Outlet />;
};

export const UsuariosLayout = () => {
  return <Outlet />;
};